README

This project was completed by Allan Sur and Jason Songvilay.

This projects aims to 'recreate' the 'ls' method found on the CLI for Linux terminals.

Proper usage:
./myls -h (optional flag to show hidden files) 'directory_name' (if left empty will display the current working directory)
